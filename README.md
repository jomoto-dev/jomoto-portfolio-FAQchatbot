# FAQ Q&A Chatbot API

## 概要

ユーザーがアップロードしたPDFを検索対象にして、質問に関連するPDF抜粋を検索し、その内容を根拠にLLMで回答を生成する FastAPI 製のWeb APIです。
ブラウザのチャット風UIからPDFをアップロードし、そのPDFの内容に対して質問できます。回答本文はOpenAI APIで自然な日本語として生成し、根拠として使ったPDFチャンクは `citations` に残します。
RAGの基本である「文書アップロード → テキスト抽出 → チャンク分割 → 関連チャンク検索 → LLM回答生成 → 引用付き回答返却」の流れを、小さく確認できるポートフォリオです。

## 作成背景

pythonを用いた実務におけるLLM/RAGアプリケーションのプロトタイプ開発を想定し、API設計からデータの前処理（テキストクリーニング・条文単位のチャンク分割）、プロンプト管理までのRAG基盤を迅速に検証・実装できる能力を証明するために作成しました
特に、単にLLMに質問を投げるのではなく、PDF本文から根拠となるチャンクを検索し、回答と引用元をあわせて返す構成にすることで、RAGアプリケーションの基本的な設計を説明できることを重視しています。

- FastAPIによるAPI実装
- PDFアップロード処理
- PDFからのテキスト抽出
- チャンク分割
- 簡易的な検索処理
- OpenAI APIを使ったLLM回答生成
- 引用元付き回答の設計
- pytestによる自動テスト
- 機能ごとのファイル分割
- 将来的なベクトル検索・複数PDF管理への拡張性

## 開発方針
このアプリは、AI協働開発を取り入れ、アーキテクチャのブレインストーミングや、プロンプトエンジニアリングによるLLM出力の制御に活用しました。ただし、生成されたコードをそのまま使うのではなく、PDF抽出やチャンク分割の適切さ、LLMによる回答生成の精度などを実際に動かして確認し、必要に応じて修正を行いながら開発を進めました。

### 設計上の制約
このアプリは提出用のローカルデモとして、単一PDF・単一プロセス・メモリ保持を前提にしています。
そのため、本番運用や複数ユーザー同時利用は想定していません。
一方で、PDF読み込み、検索、LLM回答生成、状態管理は役割ごとにファイルを分けています。これにより、将来的に複数PDF管理、DB保存、ベクトル検索、会話履歴保存などへ拡張する際に、変更箇所を整理しやすい構成にしています。

## デモイメージ

画像追加

## 使用技術と選定理由

| 分類 | 技術 | 採用理由 |
|---|---|---|
| 言語 | Python | FastAPI、PyMuPDF、OpenAI SDKなど、PDF処理やLLM連携に必要なライブラリが充実しているため。 |
| Web API | FastAPI | 少ないコードでAPIを実装でき、Swagger UIによる動作確認もしやすいため。 |
| ASGIサーバー | Uvicorn | FastAPIアプリをローカルで起動するために使用。開発時の自動リロードにも対応しているため。 |
| データ検証 | Pydantic | APIリクエストの型を明確にし、入力データの形式を管理しやすくするため。 |
| PDF処理 | PyMuPDF | PDFからテキストを抽出し、ページ番号付きのチャンク生成に利用しやすいため。 |
| LLM連携 | OpenAI Python SDK | 抽出したPDFチャンクを根拠に、自然な日本語回答を生成するため。 |
| 環境変数管理 | python-dotenv | OpenAI APIキーやモデル名を `.env` で管理し、コード内に秘密情報を直接書かないため。 |
| ファイルアップロード | python-multipart | FastAPIで `multipart/form-data` のPDFアップロードを受け取るため。 |
| テスト | pytest | API、PDFローダー、検索処理を自動テストし、修正後の動作確認をしやすくするため。 |
| フロントエンド | HTML / CSS / JavaScript | ブラウザ上でPDFアップロード、質問、回答、引用元表示を確認できる簡易UIを作るため。 |
| バージョン管理 | Git / GitHub | 開発履歴を管理し、ポートフォリオとして公開するため。 |

## API一覧

| メソッド | エンドポイント | 役割 |
|---|---|---|
| GET | `/` | チャット風UIを表示します。 |
| GET | `/health` | アプリが正常に起動しているか確認します。 |
| POST | `/upload-pdf` | PDFファイルをアップロードし、テキスト抽出・チャンク分割を行って現在の検索対象として保存します。 |
| POST | `/ask` | 質問を受け取り、アップロード済みPDFから関連チャンクを検索し、LLM回答と引用元を返します。 |

## セットアップ方法

```
#クローン作成
git clone <your-repository-url>
cd faq-rag-chatbot
#仮想環境作成
python -m venv .venv

#Windows
.venv\Scripts\activate
#Mac
source .venv/bin/activate

#インストール
pip install -r requirements.txt
```

LLM回答生成を使う場合は、`.env.example` を参考に `.env` を作成し、OpenAI APIキーと使用モデルを設定してください。`.env` はGit管理に含めない前提です。

その後、下記コマンドで起動します

```
uvicorn app.main:app --reload
```

起動後、以下にアクセスします。

- チャット風UI: <http://127.0.0.1:8000/>
- FastAPI 自動ドキュメント: <http://127.0.0.1:8000/docs>

## 使い方

1. チャット風UIを開く
2. `PDFをアップロード` からPDFファイルを送信する
3. アップロード成功後、PDFの内容に関する質問を入力する

質問例:

```text
返金について教えてください
営業時間について教えてください
領収書について教えてください
規約の同意はどのように成立する？
```

PDFをアップロードしていない状態で質問した場合は、以下のエラーが返ります。

```json
{
  "detail": "先にPDFをアップロードしてください。"
}
```

## 実装した機能

## 実装した機能

### API・UI
- `GET /health` によるヘルスチェック
- `GET /` によるチャット風UIの表示
- `POST /upload-pdf` によるPDFアップロード
- `POST /ask` による質問受付

### PDF処理
- アップロードPDFの `uploads/current.pdf` への一時保存
- PDFからのテキスト抽出
- PDF抽出テキストのクリーニング
- 条文・段落単位でのチャンク分割

### 検索・回答生成
- アップロード済みPDFチャンクに対するキーワード検索
- 関連度が高い複数チャンクの取得
- OpenAI APIを使ったPDF根拠のLLM回答生成
- `answer` と `citations` の返却

### エラーハンドリング・テスト
- PDF未アップロード、空質問、PDF以外のアップロード、抽出失敗へのエラーハンドリング
- pytestによるAPI・PDFローダー・検索処理のテスト

## 各機能の仕様紹介

### PDFアップロード機能

ブラウザUIまたは `/upload-pdf` APIからPDFをアップロードできます。アップロードされたPDFは `uploads/current.pdf` として一時保存され、テキスト抽出後にチャンクとしてメモリ上に保持されます。

このアプリでは、提出用のローカルデモとして単一PDFのみを現在の検索対象にする設計にしています。新しいPDFをアップロードすると、前回の検索対象は上書きされます。

### PDFテキスト抽出・クリーニング

PyMuPDFを使ってPDF本文を抽出します。抽出時に混ざりやすいページ番号や不要な単独行を取り除き、引用元として表示したときに読みやすい形へ整えています。

ただし、画像だけで構成されたスキャンPDFのOCRには対応していません。

### チャンク分割

PDF本文をそのまま1つの長い文章として扱うのではなく、条文や段落単位に分割します。これにより、質問に関連する部分だけを検索し、回答の根拠として提示しやすくしています。

### 関連チャンク検索

質問文から重要語を抽出し、アップロード済みPDFのチャンクと照合します。完全一致やキーワード一致に応じてスコアを付け、関連度が高いチャンクを最大3件取得します。

現時点ではベクトル検索ではなく、説明しやすいキーワードベースの簡易検索を採用しています。

### LLM回答生成

検索されたPDFチャンクを根拠としてOpenAI APIに渡し、質問に対する自然な日本語回答を生成します。回答本文とは別に、根拠となったPDFチャンクを `citations` として返すことで、回答の根拠を確認できるようにしています。

### 引用元表示

回答とあわせて、`source`, `page`, `chunk_id`, `text` を返します。これにより、回答がどのPDFのどのページ・どのチャンクに基づいているかを確認できます。

### エラーハンドリング

PDF未アップロード、空質問、PDF以外のファイルアップロード、PDFテキスト抽出失敗などに対して、利用者が原因を理解しやすいエラーメッセージを返します。


## API仕様

### `GET /health`

アプリが起動しているか確認します。

レスポンス例:

```json
{
  "status": "ok"
}
```

### `POST /upload-pdf`

PDFをアップロードし、抽出したチャンクを現在の検索対象としてメモリ上に保存します。

リクエスト形式:

```text
multipart/form-data
file: PDFファイル
```

レスポンス例:

```json
{
  "message": "PDFをアップロードしました。",
  "filename": "sample_policy_A.pdf",
  "chunk_count": 3
}
```

PDF以外をアップロードした場合:

```json
{
  "detail": "PDFファイルをアップロードしてください。"
}
```

PDFからテキストを抽出できない場合:

```json
{
  "detail": "PDFからテキストを抽出できませんでした。"
}
```

### `POST /ask`

質問を受け取り、アップロード済みPDFから関連チャンクを最大3件取得します。取得したPDF抜粋をLLMに渡し、PDF内容に基づく回答文と引用元を返します。

リクエスト例:

```json
{
  "question": "返金について教えてください"
}
```

レスポンス例:

```json
{
  "answer": "購入から7日以内であれば返金可能です。",
  "citations": [
    {
      "source": "sample_policy_A.pdf",
      "page": 1,
      "chunk_id": "chunk_001",
      "text": "第1条 返金について\n返金は購入から7日以内であれば可能です。"
    }
  ]
}
```

関連チャンクが見つからない場合:

```json
{
  "answer": "関連するPDFが見つかりませんでした。",
  "citations": []
}
```

空質問の場合:

```json
{
  "detail": "質問を入力してください。"
}
```

## アーキテクチャ

```text
ユーザー
  ↓
チャット風UI
  ↓
POST /upload-pdf
  ↓
FastAPI
  ↓
PDF Loader
  ↓
Document Store
  ↓
POST /ask
  ↓
Retriever
  ↓
LLM Client
  ↓
answer + citations
```

このアプリでは、APIの入口と内部処理を分けています。

- `app/main.py`: APIエンドポイント
- `app/schemas.py`: リクエスト形式
- `app/services/pdf_loader.py`: PDF読み込み・テキスト抽出・チャンク分割
- `app/services/retriever.py`: 関連チャンク検索
- `app/services/llm_client.py`: OpenAI APIによる回答生成
- `app/state/document_store.py`: アップロード済みPDFのチャンクをメモリ上に保持
- `app/static/index.html`: チャット風UI

## ディレクトリ構成

```text
faq-rag-chatbot/
├── app/
│   ├── __init__.py
│   ├── main.py
│   ├── schemas.py
│   ├── services/
│   │   ├── __init__.py
│   │   ├── llm_client.py
│   │   ├── pdf_loader.py
│   │   └── retriever.py
│   ├── state/
│   │   ├── __init__.py
│   │   └── document_store.py
│   └── static/
│       └── index.html
├── data/
│   ├── sample_policy_A.pdf
│   └── sample_policy_B.pdf
├── tests/
│   ├── test_api.py
│   ├── test_pdf_loader.py
│   └── test_retriever.py
├── uploads/
│   └── .gitkeep
├── .env.example
├── .gitignore
├── pyproject.toml
├── requirements.txt
└── README.md
```

`data/sample_policy_A.pdf`, `data/sample_policy_B.pdf` は動作確認用のサンプルPDFです。通常フローでは、ユーザーがアップロードしたPDFを現在の検索対象として扱います。`uploads/` は実行時にアップロードPDFを一時保存するためのフォルダで、PDF本体はGit管理対象外にしています。

・各ファイルの関係性を表した図
<img width="2222" height="1920" alt="Image" src="https://github.com/user-attachments/assets/8629305b-7be3-4fa4-b493-a6b4b024aef1" />

## テスト方法

```bash
pytest
```

期待される結果:

```text
24 passed
```

テストでは、以下を確認しています。

- `/health` が正常に応答すること
- `/` のチャット風UIが表示できること
- PDFをアップロードできること
- PDF以外のアップロードが400になること
- PDF未アップロード時の `/ask` が400になること
- アップロード後の `/ask` が `answer` と `citations` を返すこと
- `/ask` が複数の関連チャンクをLLMに渡せること
- LLM呼び出し部分をmonkeypatchし、テストで実APIを呼ばないこと
- 1文字だけの質問が誤ヒットしないこと
- 関連チャンクなしの場合に200で空の `citations` を返すこと
- PDF本文が条文・段落単位で分割されること
- PDF抽出テキストからページ番号らしき単独行を削除できること
- `第18条`, `1ヶ月前`, `7日以内` のような本文中の重要な数字は残ること
- 自然な質問文から関連チャンクを検索できること

## 実装に工夫した点

1. **アップロードPDFを現在の検索対象として扱う設計**

   複数PDF管理やDB保存は入れず、まずは `uploads/current.pdf` とメモリ上の `document_store` に絞りました。これにより、処理の流れをシンプルに保ちながら「アップロードしたPDFに対して質問する」体験を実装しています。

2. **PDF根拠のLLM回答と引用元を分ける設計**

   `answer` はLLMが生成した自然な回答文にし、根拠は `citations` として `source`, `page`, `chunk_id`, `text` を返します。回答文の中に引用記号を混ぜず、根拠確認は引用元欄で行えるようにしています。

3. **条文・段落単位のチャンク分割**

   PDFの1ページ全体を1チャンクにすると、返金・営業時間・領収書など別の話題がまとめて返ってしまいます。そのため、`第1条`, `第2条` のような見出しを区切りにし、見出しがないPDFでも空行や改行で分割するようにしています。

4. **自然な質問文に対応する複数チャンク検索**

   質問文をそのまま文字列一致させるだけでは、`規約の同意はどのように成立する？` のような自然な質問を拾いにくくなります。そのため、意味の薄い語を除外し、`規約`, `同意`, `成立` のような重要語を使ってスコアリングします。関連度が高い最大3件のチャンクをLLMへ渡すことで、回答生成に必要な根拠が届きやすくしています。

5. **PDF抽出テキストのクリーニング**

   PyMuPDFで抽出した本文には、ページ番号やフッターのような行が混ざることがあります。`2` や `- 2 -` のようなページ番号らしき単独行は削除しつつ、`第18条`, `1ヶ月前`, `7日以内` など本文中の数字は消さないようにしています。また、引用元欄で読みやすいように改行を保持しています。

6. **短すぎる質問の誤ヒット防止**

   `"あ"` や `"い"` のような1文字入力が本文中の一部に偶然一致してしまう問題を避けるため、2文字未満の質問や1文字キーワードは検索対象にしないようにしました。

7. **アップロード失敗時に前回PDFを参照しない状態管理**

   新しいPDFアップロードに失敗した場合に、直前に読み込んだPDFのチャンクが残って `/ask` で参照されないよう、アップロード処理開始時に現在の検索状態をクリアします。成功した場合のみ新しいPDFチャンクを検索対象にします。

8. **エラーハンドリングを画面にも表示**

   API側で `HTTPException` を使い、UI側では `detail` を表示します。未アップロード、空質問、PDF以外のアップロード、通信失敗などを利用者に分かりやすく返します。

9. **役割ごとのファイル分割**

   `main.py` に処理を詰め込まず、PDF読み込み、検索、LLM呼び出し、状態管理を別ファイルに分けました。今後、ベクトル検索や複数PDF管理に差し替えるときも変更箇所を追いやすくしています。

## このプロジェクトで学んだこと

- FastAPIを使って、PDFアップロード・質問受付・JSONレスポンス返却を行うWeb APIを実装する流れを学びました。
- PDFから抽出したテキストにはページ番号や不要な改行が混ざるため、RAGで使う前にクリーニングやチャンク分割が重要になることを学びました。
- LLMにそのまま質問を渡すのではなく、検索した根拠チャンクをコンテキストとして渡すことで、PDF内容に基づいた回答を生成できることを確認しました。
- 回答本文とは別に `citations` を返すことで、LLM回答の根拠をユーザーが確認できる設計にしました。
- 初期実装後に、死蔵コードの削除、FAQ表記からPDF Q&A表記への統一、LLMコンテキスト生成処理の責務移動、retrieverの重み定数化などを行い、提出前にコード構成を整理する重要性を学びました。
- pytestを使ってAPIや検索処理のテストを用意することで、リファクタリング後も既存機能が壊れていないことを確認できました。

## 現時点でできること

- ブラウザからPDFをアップロードする
- アップロードしたPDFを検索対象にする
- PDF本文から条文・段落単位のチャンクを作る
- PDF抽出テキストからページ番号らしき行を取り除く
- 質問に関連する複数チャンクを簡易検索する
- 関連チャンクを根拠にLLMで日本語回答を生成する
- LLM回答と引用元を返す
- 引用元にファイル名、ページ番号、チャンクID、本文を含める
- 回答欄と引用元欄で改行を保持して表示する
- PDFアップロード失敗後に前回PDFを検索対象として残さない
- PDF未アップロードや空質問などのエラーを返す
- pytestで主要なAPI動作を確認する

## 現時点でできないこと

- 複数PDFを同時に管理すること
- サーバー再起動後もアップロード状態を保持すること
- DBにPDFやチャンクを保存すること
- ベクトル検索による意味検索
- 会話履歴を踏まえた回答
- ストリーミング応答
- PDF以外のファイル形式への対応
- ユーザー認証や本番運用向けのログ管理
- OCRが必要なスキャンPDFの高精度な読み取り

## 今後の拡張予定

- 複数PDF管理
- チャンク分割ロジックの改善
- ベクトル検索の導入
- LLM回答の品質改善
- 会話履歴の保持
- Docker対応
- GitHub Actionsによる自動テスト

## 注意事項

- このアプリはローカル環境でのポートフォリオ用デモを想定しています。本番運用や複数ユーザーでの同時利用は想定していません。
- 現在の検索対象は、最後にアップロードされた1つのPDFです。複数PDFを同時に管理する機能はありません。
- アップロードしたPDFのチャンク情報はメモリ上に保持されるため、サーバーを再起動すると検索状態はリセットされます。
- OpenAI APIを利用する場合は、`.env` にAPIキーを設定する必要があります。`.env` はGit管理に含めないでください。
- 画像だけで構成されたスキャンPDFや、OCRが必要なPDFには対応していません。

## ライセンス

このプロジェクトはポートフォリオ用途で公開しています。  
利用・改変の条件については、必要に応じて今後ライセンスを設定します。